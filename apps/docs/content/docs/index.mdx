---
title: Introduction
description: A component registry for React, built on Base UI and Tailwind CSS.
---

Stera UI components are built on [Base UI](https://base-ui.com), styled with [Tailwind CSS v4](https://tailwindcss.com), and use [stera-icons](https://stera.sh/) for iconography. The CLI copies source directly into your project, so there's no runtime package to install or version to track. Theming uses CSS custom properties, and dark mode is included.

## Quick start

<Steps>

### Initialize

```bash
pnpm dlx stera-ui@latest init
```

### Add components

```bash
pnpm dlx stera-ui@latest add button dialog
```

### Use them

```tsx
import { Button } from "@/components/ui/button"

export function MyPage() {
  return <Button variant="brand">Click me</Button>
}
```

</Steps>

## About

Stera UI is designed for you so you can design for others. Built using Base UI &lt;3

<Callout>
    Figma -> Stera UI Bridge Plugin -> Tailwind 4 config -> Your Project
</Callout>
